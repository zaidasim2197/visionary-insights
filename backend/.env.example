# Database (Provide your MongoDB connection string)
MONGODB_URI=

# App
NODE_ENV=development
PORT=4000

# Auth
JWT_SECRET=visionpulse_super_secure_jwt_secret_sales_operations_2026_key!
JWT_EXPIRES_IN=8h

# Seed
SEED_MODE=demo
SEED_RESET=true

# Cache
KPI_CACHE_TTL_SECONDS=60

# Currency / locale
DEFAULT_CURRENCY_CODE=PKR
DEFAULT_TIMEZONE=Asia/Karachi
